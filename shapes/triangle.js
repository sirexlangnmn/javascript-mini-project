// shapes/triangle.js
export function calculateTriangleArea(base, height) {
    return 0.5 * base * height;
}

export function calculateTrianglePerimeter(side1, side2, side3) {
    return side1 + side2 + side3;
}
